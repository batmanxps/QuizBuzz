<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Forgot Password - QuizBuzz</title>
    <link rel="stylesheet" href="/styles/output.css"> <!-- Assuming Tailwind or your CSS setup -->
    <style>
        body {
            background-color: #f9fafb;
        }
    </style>
</head>
<body class="flex items-center justify-center min-h-screen">
    <div class="bg-white p-8 rounded-lg shadow-lg w-full max-w-md">
        <h2 class="text-2xl font-bold text-center mb-4 text-blue-600">Forgot Password</h2>
        <p class="text-gray-600 text-sm text-center">Enter your registered email to receive a password reset link.</p>
        <p class="text-gray-600 text-sm text-center mb-6">If you can't see the email, please check your inbox and spam.</p>

        <% if (messages.error && messages.error.length > 0) { %>
            <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-2 rounded mb-4 text-sm">
                <%= messages.error %>
            </div>
        <% } %>
        <% if (messages.success && messages.success.length > 0) { %>
            <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-2 rounded mb-4 text-sm">
                <%= messages.success %>
            </div>
        <% } %>

        <form action="/auth/forgot" method="POST" class="space-y-4">
            <div>
                <label for="email" class="block text-sm font-medium text-gray-700 mb-1">Email</label>
                <input type="email" id="email" name="email" required
                       placeholder="you@example.com"
                       class="w-full px-4 py-2 border rounded-md focus:ring-2 focus:ring-blue-500 focus:outline-none">
            </div>

            <button type="submit"
                    class="w-full bg-blue-600 text-white py-2 rounded hover:bg-blue-700 transition">
                Send Reset Link
            </button>
        </form>

        <p class="text-center text-sm text-gray-500 mt-4">
            Remember your password? <a href="/auth/login" class="text-blue-600 font-medium hover:underline">Login</a>
        </p>
    </div>
</body>
</html>
